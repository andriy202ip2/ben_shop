{% extends 'base.html.twig' %}

{% block body %}
    <h1>Automenu</h1>

    <table>
        <tbody>
            <tr>
                <th>Modelmenuid</th>
                <td>{{ autoMenu.modelMenuId }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ autoMenu.name }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ autoMenu.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('automenu_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('automenu_edit', { 'id': autoMenu.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
